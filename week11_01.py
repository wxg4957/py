# week11_01.py

class Test: #Test클래스 선언
    pass

t = Test() # 생성자 Test를 호출
           # 메모리에 Test 타입의 인스턴스(데이터) 생성
           # 그리고 해당 인스턴스의 참조값을 반환
           # 변수에 참조값 연결

i = int(1) # 생성자 int를 호출
           # 메모리에 int 타입의 인스턴스(1)를 생성
           # 그리고 해당 인스턴스의 참조값을 반환
           # 변수에 참조값 연결

print(type(t), type(i))
